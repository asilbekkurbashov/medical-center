export type doc = {
    id: string,
    image: string,
    name: string,
    title:string,
    work: string,
    address: string,
    job: string
} 

export type link = {
    label: string,
    link:string
}

export type date_hour = {
    date:string,
    hour:string[]
}